const search = (store = {}, action) => {
    switch(action.type) {
        case '??':
            return store;
        case '?':
            return store;
        default:
            return store;
    }
}

export default search;
